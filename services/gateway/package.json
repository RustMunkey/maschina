{
  "name": "@maschina/gateway",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "test": "cargo test",
    "clean": "cargo clean"
  }
}
