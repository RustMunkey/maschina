{
  "name": "@maschina/tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "e2e": "vitest run --config vitest.config.ts e2e",
    "integration": "vitest run --config vitest.config.ts integration",
    "load": "echo 'Run k6 load tests: k6 run tests/load/<script>.k6.js'",
    "clean": "rm -rf coverage"
  },
  "devDependencies": {
    "@maschina/tsconfig": "workspace:*",
    "hono": "^4.6",
    "vitest": "^4.0.18"
  }
}
