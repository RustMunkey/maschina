# PostgreSQL (Docker)
POSTGRES_USER=maschina
POSTGRES_PASSWORD=maschina
POSTGRES_DB=maschina
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# App database URL (PostgreSQL via Docker)
DATABASE_URL="postgresql://maschina:maschina@localhost:5432/maschina"

# App database URL (SQLite — no Docker needed, local dev only)
# DATABASE_URL="file:./dev.db"

# Redis
REDIS_URL="redis://localhost:6379"
